# Development / CI dependencies.
# Install with:  py -m pip install -r requirements-dev.txt
-r requirements.txt

pytest>=8.0
pytest-cov>=5.0
ruff>=0.6
mypy>=1.11

# Control UI (touchdown-analyzer serve) and the TestClient its tests use.
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27

# Windows installer (packaging\windows\build.ps1).
pyinstaller>=6.0
