# Runtime dependencies.
# Install with:  py -m pip install -r requirements.txt
#
# The capture side (probe / record / index) is deliberately stdlib-only so the
# recorder cannot fail to import at the airfield. Its one requirement is the
# ffmpeg binary on PATH, not a Python package:
#     winget install Gyan.FFmpeg
#
# The analysis side (touchdown-analyzer analyze, and the landings page of
# the UI) needs OpenCV on top. Headless: no GUI toolkit is used.
numpy>=2.0
opencv-python-headless>=4.12,<5
fastapi>=0.110
uvicorn>=0.29
