"""Putting a registration on a landing (docs/design.md 3.4).

The camera cannot read one at this distance, so identification comes from
the sequence of events instead: an OGN/FLARM track that was at the field
at the touchdown instant, or a logbook entry with a matching landing time.
The judge confirms or corrects in the browser; that is the fallback under
every scheme.
"""
