# Copy to .env and fill in. .env itself is git-ignored.
#
# Camera stream. Written automatically the first time you pass --source (or
# tick 'remember' in the UI); afterwards --source can be omitted.
# CAMERA_URL=rtsp://root:PASSWORD@192.168.200.222/axis-media/media.amp
